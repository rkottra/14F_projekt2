import { KosarbeliTermekModel } from './kosarbeli-termek.model';

describe('KosarbeliTermekModel', () => {
  it('should create an instance', () => {
    expect(new KosarbeliTermekModel()).toBeTruthy();
  });
});
